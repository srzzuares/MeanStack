const mongoose = require('mongoose')
require('dotenv').config()
mongoose.connect(process.env.MONGODB)
    .then(db => console.log('Connected to mongo HUMAN!!!!'))
    .catch(err => console.err(err))
